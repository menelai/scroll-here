import { ScrollHereDirective } from './scroll-here.directive';

describe('ScrollHereDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollHereDirective();
    expect(directive).toBeTruthy();
  });
});
