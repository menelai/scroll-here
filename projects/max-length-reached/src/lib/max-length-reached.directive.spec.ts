import { MaxLengthReachedDirective } from './max-length-reached.directive';

describe('MaxLengthReachedDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxLengthReachedDirective();
    expect(directive).toBeTruthy();
  });
});
